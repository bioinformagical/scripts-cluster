#!/bin/bash
# Request a run time
#PBS -l walltime=720:00:00

# Request 1 processor in 1 node
#PBS -l nodes=1:ppn=64

# Request 7600 megabytes memory per processor.  ( 48 usable CPUs)
#PBS -l mem=950gb
#PBS -l pmem=8gb
#PBS -j oe
#PBS -N aaabyssmal-mam44
#PBS -q hammerhead

IFS=$'\n'          # Internal Field Separator is newline
set -eu
umask 007
shopt -s nullglob  # Treat empty glob (*) as nothing instead of empty string
ulimit -c 10240
ulimit -a

###Start DOING STUFF

cd /nobackup/rogers_research/DeNovoMammoth/abyss-44

module load mpiblast/1.6.0
module load abyss/1.9.0

#ABYSS-P -k28 -q 3 -np 32 -o mam44 ../ERR855944.fastq.gz

abyss-pe np=32  k=23 se='/nobackup/rogers_research/DeNovoMammoth/ERR855944.fastq.gz' name="m44" 



